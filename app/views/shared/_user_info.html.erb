<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= link_to current_user.name, current_user, class: "user-info-partial" %>
</h1>
<span>
  <%= pluralize(current_user.items.count, "item") %>
</span>
