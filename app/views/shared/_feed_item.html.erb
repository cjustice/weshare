<li id="<%= feed_item.id %>">
		<%= image_tag(feed_item.avatar.url(:medium), alt: feed_item.title, class: "item-image img-rounded") %>
	</br>
		<strong><span class="title"><%= feed_item.title %></span></strong>
		<button type="button" class="btn btn-success borrow-btn">Borrow!</button>

</li>